/*
Ray Charles-Blackjack, 

Luck Be A Lady, Frank Sinatra

return String.Join(Environment.NewLine, this.deck.Select(x => x.ToString()).ToArray())
Konverterar från array till string från v3.5, Select är en automatisk for-loop(eller foreach) som gör det för varje värde i en array.
  
(new SoundPlayer(soundFile)).PlaySync();
PlayLooping()
Play och synk direkt.

var card = cards.Count(c => c.Rank == Rank.card);
Räkna kort av en viss valör, kan användas för soft.

1 : 0 = bestämmer var värdet ska läggas till.
*/

